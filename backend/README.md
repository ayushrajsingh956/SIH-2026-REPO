# LegalMetro Shield — Backend API & Worker

FastAPI backend and Celery workers for automated compliance checking under Legal Metrology (Packaged Commodities) Rules, 2011.

## Quickstart

```bash
uv sync --all-extras
uv run uvicorn app.main:app --reload --port 8000
```
